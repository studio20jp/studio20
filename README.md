# studio20 website

静的HTMLサイトです。GitHub Pagesへそのまま公開できます。

## 公開方法
1. このフォルダ内のファイルをGitHubリポジトリの最上位へアップロード
2. Settings → Pages
3. Branch を `main`、フォルダを `/ (root)` に設定

## 公開前に変更する箇所
- `index.html` のInstagramリンク
- `index.html` のメールアドレス `info@example.com`

画像はWebPへ圧縮済みです。
